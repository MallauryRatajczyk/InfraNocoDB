stages:
  - validate
  - test
  - build
  - deploy
  - cleanup

include:
  - template: 'Terraform/Base.gitlab-ci.yml'

fmt:
  extends: .terraform:fmt
  needs: []

validate:
  extends: .terraform:validate
  needs: []

build:
  extends: .terraform:build
  environment:
    name: $TF_STATE_NAME
    action: prepare
  before_script:
    - mkdir .ssh
    - echo $CI_KEY_SSH > .ssh/id_rsa.pub
    - echo "{ name = \"$CI_TERRAFORM_USER\"" > terraform.tfvars
    - echo " password = \"$CI_TERRAFORM_PASS\"}" >> terraform.tfvars
    - cat terraform.tfvars
  script:
    - gitlab-terraform plan -var-file=terraform.tfvars -var 'ssh_user=$CI_USER_SSH'
    - gitlab-terraform plan-json

deploy:
  extends: .terraform:deploy
  dependencies:
    - build
  environment:
    name: $TF_STATE_NAME
    action: start